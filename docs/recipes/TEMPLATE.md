# Recipe: <concept> in <backend>

> The recipe is the actual product of this lab. Code that works is table stakes; code
> a reader understands is the deliverable. Every recipe follows this shape and must be
> reproducible by someone who only cloned the repo.

## 1. Problem

What pain in the ticketing domain does this solve? Be concrete. "Two people click buy
on the last ticket at the same millisecond" beats "concurrency is hard."

## 2. Concept

The idea, explained without reference to any language or framework. If a reader
understands this section, they could implement it themselves anywhere.

## 3. Implementation

Real code from this repository, with file paths so the reader can open it. Show the
interesting part, not the boilerplate. Link to the full file rather than pasting it.

```
// path/to/the/real/file.ext
```

## 4. How to see it working

Exact commands. What to observe: a metric on a Grafana panel, a log line, a trace
span, a test that goes red without the fix and green with it. Seeing beats believing.

## 5. Trade-offs

What this choice costs, and when you would not reach for it. A recipe with no
trade-offs section is marketing, not engineering.
